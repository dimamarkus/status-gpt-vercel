import { Font, Layout, Theme } from '#/lib/contexts/FeatureToggleContext';

export const DEFAULT_FEATURES = {
  font: 'graphik' as Font,
  layout: 'veritcal' as Layout,
  theme: 'statusMoney' as Theme,
  promptDebug: false,
};
